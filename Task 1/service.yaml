apiVersion: v1
kind: Service
metadata:
  name: cicd-demo-api-svc
  namespace: default
  labels:
    app: cicd-demo-api
spec:
  type: NodePort
  selector:
    app: cicd-demo-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
